Requires
 - PySpark (pip install pyspark) 
 - MySQL (sudo apt install mysql-server)
 - install jdbc driver from MySQL website
 - python3 -m pip install mysql-connector-python
 
 
Post notes:
 - replace my name in configuration strings
 - no schema stuff because it conflicts with database read
